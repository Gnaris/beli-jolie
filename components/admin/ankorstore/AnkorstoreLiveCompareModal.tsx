"use client";

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export default function AnkorstoreLiveCompareModal(_props: { productId: string; initialData: any; open: boolean; onClose: () => void }) {
  if (!_props.open) return null;
  return <div>Compare modal placeholder</div>;
}
